apiVersion: v1
kind: Config
clusters:
- name: local
  cluster:
    certificate-authority: /etc/kubernetes/ssl/workerworkerID/ca.pem
users:
- name: kubelet
  user:
    client-certificate: /etc/kubernetes/ssl/workerworkerID/worker.pem
    client-key: /etc/kubernetes/ssl/workerworkerID/worker-key.pem
contexts:
- context:
    cluster: local
    user: kubelet
  name: kubelet-context
current-context: kubelet-context
